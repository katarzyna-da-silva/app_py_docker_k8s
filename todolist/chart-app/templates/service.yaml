apiVersion: v1
kind: Service
metadata:
  name: {{ include "chart_app.fullname" . }}
  labels:
    {{- include "chart_app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chart_app.selectorLabels" . | nindent 4 }}
